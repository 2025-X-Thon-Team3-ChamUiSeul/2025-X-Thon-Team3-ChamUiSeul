# --- Global ---
.DS_Store
*.log
.env
**/.env             # <- 이렇게 추가하면 모든 폴더의 .env를 무시합니다 (추천)
backend/.env     # <- 혹은 이렇게 특정 경로만 추가해도 됩니다
venv/
backend/venv/

# --- Frontend (Node/React) ---
frontend/node_modules/
frontend/.next/
frontend/build/
frontend/dist/
frontend/.env
frontend/.env.local

# 1. 파이썬 필수 제외 (캐시 파일)
__pycache__/
*.pyc
*.pyo
*.pyd

# 2. 가상환경 (내 컴퓨터용 공구함)
venv/
backend/venv/
env/
.venv/

# 3. 비밀번호 및 환경설정 (보안)
.env
.env.*
backend/.env

# 4. 데이터베이스 (로컬 파일 & 벡터 DB)
*.db
*.sqlite3
chroma_db/

# 5. 운영체제 잡동사니 (맥/윈도우)
.DS_Store
Thumbs.db

# 6. VS Code 설정 (팀원끼리 설정 다를 수 있음)
.vscode/

# --- Backend (FastAPI/Python) ---
backend/fastapi-ai-agent/__pycache__/
backend/fastapi-ai-agent/venv/
backend/fastapi-ai-agent/.env
backend/fastapi-ai-agent/*.pyc

# --- Secret Keys (절대 올리지 말 것) ---
secrets.json
key.pem